"use strict";

//=============================calculating the Area of a circle========================================

const radius = "10px";
const pi = Math.PI;
const area = pi * Number.parseInt(radius) ** 2;
console.log(area);
